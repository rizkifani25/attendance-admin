import 'package:flutter/material.dart';

class LecturerView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('lecturer view'),
    );
  }
}
